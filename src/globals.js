export const globals = (() => {
  const turn = 'X';
  let player1;
  let player2;
  const continueGame = true;

  return {
    turn,
    player1,
    player2,
    continueGame,
  };
})();

export default globals;